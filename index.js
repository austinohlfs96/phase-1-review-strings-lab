// Write your code inthis file!
let currentUser = `You need to modify the value of the ${currentUser} variable`;

let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

let upperCaseCurrentUser = currentUser.toUpperCase();

let excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${upperCaseCurrentUser}!`;

const firstInital = currentUser[0];
const restOfName = currentUser.slice(1);

let shortGreeting = `Welcome, ${firstInital}!`;